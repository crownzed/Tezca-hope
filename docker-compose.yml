services:
  tezca-api:
    build: ./server
    container_name: tezca-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      JWT_SECRET: ${JWT_SECRET:-tezca-change-me-in-production}
      DATA_DIR: /app/data
      GOOGLE_GENERATIVE_AI_API_KEY: ${GOOGLE_GENERATIVE_AI_API_KEY:-}
      GOOGLE_GENERATIVE_AI_MODEL: ${GOOGLE_GENERATIVE_AI_MODEL:-gemini-2.5-flash}
    volumes:
      - tezca-api-data:/app/data

  tezca-web:
    build: .
    container_name: tezca-web
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - tezca-api

volumes:
  tezca-api-data:
