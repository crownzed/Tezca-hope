rules_version = '2';

// Cộng đồng chỉ qua API Tezca (Firebase Admin SDK). Client web không đọc/ghi Firestore trực tiếp.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
